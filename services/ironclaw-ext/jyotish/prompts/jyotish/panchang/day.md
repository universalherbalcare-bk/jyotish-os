Use for 'today's panchang', tithi/nakshatra end times, hora, rahu kala or abhijit questions. Requires a calendar date plus lat/lon/tz offset. Report transition instants as returned (UTC) and convert to local time only using the supplied tz_offset_hours. Every result carries `evidence` {engine, kernel, ayanamsa, consensus_status, delta_t_sigma_sec}. Quote `evidence.consensus_status` in your answer. If the tool returns an error, report it verbatim; never substitute a number.
