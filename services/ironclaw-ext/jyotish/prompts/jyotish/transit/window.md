Use for 'what transit changes this week/month' and Sade Sati questions. Provide from_utc/to_utc bounding the window (keep it <= 1 year). Instants are exact; present them with the user's tz offset. Every result carries `evidence` {engine, kernel, ayanamsa, consensus_status, delta_t_sigma_sec}. Quote `evidence.consensus_status` in your answer. If the tool returns an error, report it verbatim; never substitute a number.
