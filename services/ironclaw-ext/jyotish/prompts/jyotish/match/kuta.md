Use for compatibility/matching questions. Needs two complete BirthInput objects (`a`, `b`). Present both engines' totals; if they disagree, say so rather than averaging. Every result carries `evidence` {engine, kernel, ayanamsa, consensus_status, delta_t_sigma_sec}. Quote `evidence.consensus_status` in your answer. If the tool returns an error, report it verbatim; never substitute a number.
