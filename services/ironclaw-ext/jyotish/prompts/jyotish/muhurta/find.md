Use for 'when should I ...' questions. `activity` is a VedAstro rule Name (e.g. GoodLunarDayForTravel); list options via catalog.list or config/activities.json. Always show the passed_rules and vetoed_by ids for each window so a human astrologer can audit it. Every result carries `evidence` {engine, kernel, ayanamsa, consensus_status, delta_t_sigma_sec}. Quote `evidence.consensus_status` in your answer. If the tool returns an error, report it verbatim; never substitute a number.
