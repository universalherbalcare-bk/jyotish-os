Use to audit engine agreement (nightly heartbeat runs it on the golden chart). Report every body's delta and the overall status; a FAIL means the tools are not to be trusted until fixed. Every result carries `evidence` {engine, kernel, ayanamsa, consensus_status, delta_t_sigma_sec}. Quote `evidence.consensus_status` in your answer. If the tool returns an error, report it verbatim; never substitute a number.
