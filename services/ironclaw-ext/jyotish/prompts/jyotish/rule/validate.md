Use before relying on any yoga/dosha rule for a prediction. Quote n, hit_rate, base_rate, ci95 and the verdict verbatim; a KEEP_UNPROVED rule must be presented as unproved. This tool appends to the promotion log, so confirm with the user before running it in bulk. Every result carries `evidence` {engine, kernel, ayanamsa, consensus_status, delta_t_sigma_sec}. Quote `evidence.consensus_status` in your answer. If the tool returns an error, report it verbatim; never substitute a number.
