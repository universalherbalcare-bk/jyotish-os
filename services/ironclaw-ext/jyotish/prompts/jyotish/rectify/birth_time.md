Use only when the user asks to rectify an uncertain birth time. Requires the nominal birth, a +/- window in minutes, and at least one dated life event. State plainly that the result is a heuristic ranking, not a determination, and quote each candidate's score. Every result carries `evidence` {engine, kernel, ayanamsa, consensus_status, delta_t_sigma_sec}. Quote `evidence.consensus_status` in your answer. If the tool returns an error, report it verbatim; never substitute a number.
