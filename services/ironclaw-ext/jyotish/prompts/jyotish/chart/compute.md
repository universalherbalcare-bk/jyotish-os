Use for any natal or divisional chart question. Pass the `birth` object exactly as the user supplied it (UTC instant + lat/lon + tz offset); never geocode or guess coordinates. Read `boundary_distance_sec` on every rashi/nakshatra/pada: when it is below 120 seconds, say so explicitly because a one-minute birth-time error would flip that value. Every result carries `evidence` {engine, kernel, ayanamsa, consensus_status, delta_t_sigma_sec}. Quote `evidence.consensus_status` in your answer. If the tool returns an error, report it verbatim; never substitute a number.
