# JYOTISH-OS — session state
Objective: build all 5 blueprint phases and run real tests (2026-09-16).
Decisions: project at ~/Projects/jyotish-os (own git repo); vendor/ gitignored + restorable via scripts/vendor.sh; contract in docs/CONTRACT.md; jyotish-mcp serves HTTPS on loopback (Phase 6: scripts/gen-cert.sh + scripts/make-ca-bundle.sh, certs/ gitignored, JYOTISH_TLS=off for tests).
Phase status: P1 ✅ · P2 ✅ · P3 ✅ · P4 ✅ (corpus PASS) · P5/P6 ◐ (https loopback + cert trust solved) · **P7 ✅ — a real `catalog.list` and `chart.compute` (GOLDEN, consensus PASS, Moon Swati) completed through IronClaw** over loopback TLS with the patched `~/.local/bin/ironclaw-jyotish` (source: vendor/ironclaw-fork @ ironclaw-v1.4.0, patch: patches/ironclaw-1.4.0-allow-loopback-egress.patch, opt-in `IRONCLAW_EGRESS_ALLOW_LOOPBACK=1`, 4 files +206/-14, crate tests green 24+1+17+17 and 489+9+7+2). Root cause corrected: the P6 `network_denied` was gate 1 (filesystem-installed `[mcp]` packages are never hosted-MCP providers in 1.4.0 → empty egress allow-list), not the private-IP guard; the callable extension is the registered `mcp-jyotish-local` (`install.sh --register`, 10 tools discovered live). Negative controls pass (flag unset → policy_denied; 192.168.x / 10.0.0.5 denied with the flag). Homebrew ironclaw untouched; rollback = rm ~/.local/bin/ironclaw-jyotish. Nothing committed (P7 changes are in the working tree). Provider still anthropic / claude-sonnet-5, key NOT entered.
Next: owner review of P7 (uncommitted) → commit; store Claude key (`ironclaw config set anthropic.api_key`) and run a real agent turn with `IRONCLAW_EGRESS_ALLOW_LOOPBACK=1 SSL_CERT_FILE=certs/ironclaw-ca-bundle.pem ~/.local/bin/ironclaw-jyotish repl`; then routines (services/ironclaw-ext/routines/README.md).
